<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CsWinRTApp.Pages.Page2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CsWinRTApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Padding="16" Spacing="8">
            <Button Content="返回" Click="BackButton_Click"/>
            <TextBlock x:Name="FileNameText" VerticalAlignment="Center" FontSize="16" FontWeight="SemiBold"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="ContentGrid" Padding="16">
                <Image x:Name="ImageViewer" Visibility="Collapsed" Stretch="Uniform"/>
                <TextBlock x:Name="TextViewer" Visibility="Collapsed" TextWrapping="Wrap" FontFamily="Consolas"/>
                <StackPanel x:Name="FileInfoViewer" Visibility="Collapsed" Spacing="12">
                    <TextBlock x:Name="FileInfoTitle" FontSize="20" FontWeight="Bold"/>
                    <TextBlock x:Name="FileInfoSize"/>
                    <TextBlock x:Name="FileInfoPath" TextWrapping="Wrap"/>
                    <TextBlock x:Name="FileInfoExtension"/>
                    <TextBlock x:Name="FileInfoCreated"/>
                    <TextBlock x:Name="FileInfoModified"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
