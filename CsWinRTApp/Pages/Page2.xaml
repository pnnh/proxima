<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CsWinRTApp.Pages.Page2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CsWinRTApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="8,4" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}" 
              BorderThickness="0,0,0,1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="4">
                <Button x:Name="BackButton" Click="BackButton_Click">
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE72B;" FontSize="16"/>
                        <TextBlock Text="返回" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Back"/>
                    </Button.KeyboardAccelerators>
                </Button>
            </StackPanel>

            <TextBlock Grid.Column="1" 
                       x:Name="FileNameText" 
                       VerticalAlignment="Center" 
                       Margin="16,0,0,0"
                       FontSize="16" 
                       FontWeight="SemiBold"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="4" x:Name="ImageControlPanel" Visibility="Collapsed">
                <Button x:Name="FitToWindowButton" 
                        Click="FitToWindowButton_Click"
                        ToolTipService.ToolTip="适应窗口">
                    <FontIcon Glyph="&#xE740;" FontSize="16"/>
                </Button>
                <Button x:Name="ActualSizeButton" 
                        Click="ActualSizeButton_Click"
                        ToolTipService.ToolTip="实际大小">
                    <FontIcon Glyph="&#xE71E;" FontSize="16"/>
                </Button>
            </StackPanel>
        </Grid>

        <ScrollViewer x:Name="ContentScrollViewer" 
                      Grid.Row="1" 
                      HorizontalScrollBarVisibility="Auto" 
                      VerticalScrollBarVisibility="Auto"
                      ZoomMode="Disabled">
            <Grid x:Name="ContentGrid" Padding="16">
                <Image x:Name="ImageViewer" 
                       Visibility="Collapsed" 
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
                <WebView2 x:Name="WebViewer" 
                          Visibility="Collapsed"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"/>
                <TextBlock x:Name="TextViewer" 
                           Visibility="Collapsed" 
                           TextWrapping="Wrap" 
                           FontFamily="Consolas"/>
                <StackPanel x:Name="FileInfoViewer" 
                            Visibility="Collapsed" 
                            Spacing="12"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top">
                    <TextBlock x:Name="FileInfoTitle" FontSize="20" FontWeight="Bold"/>
                    <TextBlock x:Name="FileInfoSize"/>
                    <TextBlock x:Name="FileInfoPath" TextWrapping="Wrap"/>
                    <TextBlock x:Name="FileInfoExtension"/>
                    <TextBlock x:Name="FileInfoCreated"/>
                    <TextBlock x:Name="FileInfoModified"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
