<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme — Material Design 3 -->
    <style name="Theme.Venus" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>

    <!-- Inherits NoActionBar from parent; left as empty override slot -->
    <style name="Theme.Venus.NoActionBar" />

    <style name="Theme.Venus.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.Venus.PopupOverlay"  parent="ThemeOverlay.AppCompat.Light" />
</resources>